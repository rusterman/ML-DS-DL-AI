# ✅ Math & ML & DS & DL & AI

This project demonstrates how to solve different types of linear equations using Python.

## 📦 Requirements

Before running the code, set up a virtual environment to avoid system dependency issues.

### 🔧 Setup with `venv` (Recommended)

```bash
# Create virtual environment
python3 -m venv venv

# Activate it (macOS/Linux)
source venv/bin/activate

# Install required packages
pip install -r requirements.txt
